; generated by drom from package skeleton 'driver'
(executable
  (name main)
  (public_name ez-subst)
  (package ez-subst)
  (libraries toml ocplib_stuff ez_subst ez_file )
  
  
  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package ez-subst))


